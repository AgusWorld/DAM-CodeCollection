<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#2C2C2C"
    tools:context=".MainActivity">


    <TextView
        android:layout_width="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_height="wrap_content"
        android:textSize="22dp"
        android:id="@+id/PlayListNameTextView"
        android:textColor="@color/white"
        android:text="Future Nostalgia"
        android:padding="14dp"
        android:textFontWeight="700"
        />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/white"
        android:id="@+id/DidntFindSongTextView"
        android:text="Couldn't find any song"
        android:layout_centerInParent="true"
        android:visibility="gone"
        />


    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Retry"
        android:layout_below="@id/DidntFindSongTextView"
        android:id="@+id/RetryToFindSongButton"
        android:layout_centerInParent="true"
        android:visibility="gone"
        />

    <ListView        android:id="@+id/PlayListSongsView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/PlayListNameTextView"
        android:textColor="@color/white" />

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:id="@+id/Extras"
        android:paddingBottom="10dp"
        android:paddingHorizontal="30dp">
        <Button
            android:id="@+id/btnCamera"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="✌😎📸"
            android:layout_alignParentStart="true"
            android:padding="10dp"
            />
        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="NUEVA SESSION🎙"
            android:id="@+id/btn_record"
            android:layout_alignParentEnd="true"
            android:padding="10dp"/>
    </RelativeLayout>


</RelativeLayout>